version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      - run:
        name: Build Container
        command: make build
      - run:
        name: Run Tests
        command: make test
